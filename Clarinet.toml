[project]
name = 'TreeGrow'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.reward-distribution]
path = 'contracts/reward-distribution.clar'
clarity_version = 3
epoch = 'latest'

[contracts.tree-registry]
path = 'contracts/tree-registry.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
